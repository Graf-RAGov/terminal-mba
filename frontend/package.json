{
  "name": "terminalmba-frontend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/build.ts",
    "build": "bun run src/build.ts",
    "preview": "bun run build && bunx serve dist"
  },
  "devDependencies": {
    "typescript": "^5.4"
  }
}
